{
  "name": "asset-pipeline-ci",
  "version": "0.1.0",
  "description": "Shared CI workflows for the prompt-asset toolchain",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^25.9.1",
    "vitest": "^2.1.0"
  }
}
